# План разработки Flowaxy CMS

**Версия ядра:** 7.0.0  
**Цель:** Довести ядро движка до финальной стабильной версии для крупных проектов  
**Дата начала:** 2024  
**Последнее обновление:** 2025-01-21

---

## 📋 Текущий статус проекта

### ✅ Реализованные компоненты

#### Ядро системы
- [x] Базовая архитектура и структура проекта
- [x] Система автозагрузки классов (autoloader)
- [x] Инициализация системы (`engine/init.php`)
- [x] Точка входа (`index.php`)
- [x] **Система менеджеров хранилища** (CookieManager, SessionManager, StorageManager)
- [x] **RouterManager** - умный роутинг с определением контекста
- [x] **Блокировка установщика** после установки системы

#### Работа с данными
- [x] Класс `Database` - абстракция БД через PDO
- [x] Класс `Cache` - система кеширования
- [x] Класс `Logger` - логирование
- [x] Класс `Config` - конфигурация
- [x] **StorageInterface** - единый интерфейс для всех хранилищ
- [x] **StorageFactory** - фабрика для создания менеджеров хранилища

#### Файловая система
- [x] Класс `File` - работа с файлами
- [x] Класс `Directory` - работа с директориями
- [x] Класс `Upload` - загрузка файлов
- [x] Класс `Image` - обработка изображений
- [x] Класс `Zip` - архивация
- [x] Классы для работы с форматами: `Json`, `Xml`, `Yaml`, `Ini`, `Csv`
- [x] Класс `MimeType` - определение MIME-типов

#### HTTP-слой
- [x] Класс `Request` - обработка запросов
- [x] Класс `Response` - формирование ответов
- [x] Класс `Router` - маршрутизация
- [x] Класс `AjaxHandler` - AJAX-обработка
- [x] Класс `Cookie` - работа с cookies
- [x] **Security Headers** - защита от XSS, clickjacking, MIME sniffing
- [x] **WebhookDispatcher** - отправка webhooks
- [x] **ApiHandler** - аутентификация API запросов

#### Безопасность
- [x] Класс `Security` - основные функции безопасности
- [x] Класс `Hash` - хеширование
- [x] Класс `Encryption` - шифрование
- [x] Класс `Session` - управление сессиями (используется SessionManager)
- [x] Хелперы: `SecurityHelper`, `DatabaseHelper`, `UrlHelper`
- [x] **Защита от SQL injection:** 214+ запросов с prepared statements
- [x] **Защита от XSS:** 301+ использований htmlspecialchars
- [x] **CSRF защита:** Все формы защищены токенами
- [x] **Security Headers:** CSP, X-Frame-Options, X-Content-Type-Options, HSTS
- [x] **Система ролей:** RoleManager с проверкой прав доступа

#### Валидация и UI
- [x] Класс `Validator` - валидация данных
- [x] Класс `ModalHandler` - модальные окна
- [x] ~~Класс `MenuManager` - управление меню~~ (перенесено в плагин `menu-plugin`)

#### Менеджеры системы
- [x] `PluginManager` - менеджер плагинов
- [x] `ThemeManager` - менеджер тем
- [x] `SettingsManager` - управление настройками
- [x] `ThemeCustomizer` - кастомизация тем
- [x] `RoleManager` - управление ролями и правами
- [x] `RouterManager` - умный роутинг
- [x] `ApiManager` - управление API ключами
- [x] `WebhookManager` - управление webhooks
- [x] `CookieManager` - управление cookies
- [x] `SessionManager` - управление сессиями
- [x] `StorageManager` - управление клиентским хранилищем
- [x] `StorageFactory` - фабрика менеджеров хранилища
- [x] `InstallerManager` - управление установкой

#### Базовые классы
- [x] `BaseModule` - базовый класс модулей
- [x] `BasePlugin` - базовый класс плагинов
- [x] ~~`ThemePlugin` - базовый класс плагинов тем~~ (удален, функциональность в ThemeManager)

#### Административная панель
- [x] Базовая структура админки (`engine/skins/`)
- [x] Базовый класс `AdminPage`
- [x] Система компонентов UI (16 компонентов)
- [x] Страницы:
  - [x] Dashboard (главная панель)
  - [x] Plugins (управление плагинами)
  - [x] Themes (управление темами)
  - [x] Settings (настройки)
  - [x] Site Settings (настройки сайта)
  - [x] Roles (роли и права доступа)
  - [x] Users (управление пользователями)
  - [x] Profile (профиль)
  - [x] API Keys (управление API ключами)
  - [x] Webhooks (управление webhooks)
  - [x] Cache View (просмотр кеша)
  - [x] Logs View (просмотр логов)
  - [x] Customizer (кастомизатор тем)
  - [x] Theme Editor (редактор тем)
  - [x] ~~Menus (управление меню)~~ (перенесено в плагин `menu-plugin`)
  - [x] Login/Logout

#### Система шаблонизации
- [x] Класс `View` - шаблонизация
- [x] `SimpleTemplate` - упрощенный шаблонизатор для админки
- [x] Базовые макеты (`base.php`, `base-plugin.php`)

#### Дополнительные компоненты
- [x] `ScssCompiler` - компиляция SCSS
- [x] `Mail` - отправка почты
- [x] JavaScript API для StorageManager

---

## 🎯 Задачи для финальной версии ядра

### 🔴 Критичные задачи (Must Have)

#### 1. Система хуков и событий ✅
- [x] Реализация полноценной системы хуков
  - [x] Регистрация хуков в плагинах
  - [x] Фильтры для модификации данных
  - [x] События для выполнения действий
  - [x] Приоритизация хуков
  - [x] Условное выполнение хуков
  - [x] Документация по хукам

#### 2. Система миграций БД
- [ ] Класс для управления миграциями
- [ ] Автоматическое выполнение миграций при установке плагинов
- [ ] Откат миграций при удалении плагинов
- [ ] Версионирование миграций
- [ ] Структура директорий `migrations/` в плагинах

#### 3. Система ролей и прав доступа ✅
- [x] Класс `RoleManager` для управления ролями
- [x] Система разрешений (permissions)
- [x] Проверка прав доступа в админке
- [x] API для проверки прав в плагинах
- [x] Интеграция в кабинет
- [x] Глобальные функции для работы с ролями
- [x] Упрощенная система ролей (Guest, User, Developer)
- [x] Хранение ролей в JSON колонке `role_ids` таблицы `users`

#### 4. Безопасность для крупных проектов ✅ (частично)
- [x] Security Headers (CSP, X-Frame-Options, X-Content-Type-Options, HSTS)
- [x] Защита от SQL injection (prepared statements)
- [x] Защита от XSS (htmlspecialchars везде)
- [x] CSRF защита (токены во всех формах)
- [ ] Rate limiting для API запросов
- [ ] Защита от DDoS атак
- [ ] Логирование подозрительной активности
- [ ] Автоматическое блокирование подозрительных IP
- [ ] Двухфакторная аутентификация (2FA)

#### 5. API для разработчиков
- [x] Базовые API endpoints (info, status, me, permissions)
- [x] ApiManager для управления API ключами
- [x] ApiHandler для аутентификации
- [ ] Полная документация API (Swagger/OpenAPI)
- [ ] Примеры использования API
- [ ] SDK для популярных языков (JavaScript, Python, PHP)
- [ ] Ограничение rate limits для API ключей
- [ ] Webhooks для интеграций

#### 6. Тестирование и стабильность
- [ ] Unit-тесты для критичных компонентов (PHPUnit)
- [ ] Интеграционные тесты
- [ ] Тестирование установки/удаления плагинов
- [ ] Тестирование работы с темами
- [ ] Автоматическая проверка безопасности (OWASP)
- [ ] Performance тесты

### 🟡 Важные задачи (Should Have)

#### 7. Документация для разработчиков
- [x] Документация по системе хуков (`.dev/wiki/engine/classes/system/HOOKS_DOCUMENTATION.md`)
- [x] Документация по менеджерам хранилища (`.dev/docs/STORAGE_MANAGERS.md`)
- [x] Примеры плагинов и тем
- [ ] **Полное руководство по разработке плагинов** (обязательно)
- [ ] **Полное руководство по разработке тем** (обязательно)
- [ ] API Reference (автогенерация из PHPDoc)
- [ ] Примеры кода для всех основных функций
- [ ] Video tutorials для начинающих
- [ ] FAQ для разработчиков

#### 8. Система миграций для плагинов
- [ ] Класс `MigrationManager` для плагинов
- [ ] Автоматическое выполнение миграций при активации
- [ ] Откат миграций при деактивации
- [ ] Версионирование миграций
- [ ] Проверка совместимости миграций

#### 9. Расширенная система логирования
- [x] Уровни логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
- [x] Ротация логов по размеру и времени
- [x] Фильтрация логов в админке (по уровню, дате, модулю)
- [x] Экспорт логов

#### 10. Система обновлений
- [ ] Проверка обновлений ядра
- [ ] Автоматическое обновление плагинов
- [ ] Резервное копирование перед обновлением
- [ ] Откат обновлений
- [ ] Changelog для каждого обновления

#### 11. Мультиязычность (i18n)
- [ ] Система переводов
- [ ] Локализация админки
- [ ] API для переводов в плагинах
- [ ] Выбор языка в настройках
- [ ] Автоматическое определение языка пользователя

#### 12. Оптимизация производительности
- [x] Система кеширования с настраиваемым TTL
- [ ] Профилирование кода (Xdebug, Blackfire)
- [ ] Оптимизация запросов к БД (query optimization)
- [ ] Кеширование запросов к БД
- [ ] Ленивая загрузка компонентов
- [ ] Минификация CSS/JS для продакшена
- [ ] Сжатие ответов (gzip/brotli)

### 🟢 Желательные задачи (Nice to Have)

#### 13. Дополнительные возможности
- [x] Система виджетов для Dashboard
- [ ] Расширенный редактор тем (code editor с подсветкой синтаксиса)
- [ ] Визуальный редактор меню (drag & drop)
- [ ] Импорт/экспорт настроек
- [ ] Система резервного копирования
- [ ] Планировщик задач (Cron jobs)
- [ ] Очередь задач (Queue system)

#### 14. Улучшение UX админки
- [ ] Drag & Drop для виджетов
- [ ] Темная тема (dark mode)
- [ ] Настройка интерфейса (персонализация)
- [ ] Клавиатурные сокращения
- [ ] Поиск по админке
- [ ] Горячие клавиши для действий
- [ ] Мультиязычный интерфейс

#### 15. Интеграции
- [x] API для внешних приложений
- [x] Webhooks для событий системы
- [ ] Интеграция с популярными сервисами (Stripe, PayPal, Google Analytics)
- [ ] OAuth авторизация (Google, Facebook, GitHub)
- [ ] SSO (Single Sign-On) поддержка

---

## 🔒 Безопасность для крупных проектов

### ✅ Реализовано

1. **SQL Injection защита**
   - ✅ Все запросы используют prepared statements (214+ запросов)
   - ✅ Параметризованные запросы везде
   - ✅ Валидация входных данных перед запросами

2. **XSS защита**
   - ✅ Экранирование вывода через `htmlspecialchars()` (301+ использований)
   - ✅ `Security::clean()` для очистки данных
   - ✅ `SecurityHelper::sanitizeInput()` для всех входных данных

3. **CSRF защита**
   - ✅ CSRF токены во всех формах
   - ✅ `SecurityHelper::csrfToken()` и `csrfField()`
   - ✅ Проверка токенов на всех POST запросах

4. **Аутентификация и авторизация**
   - ✅ Безопасное хеширование паролей (`password_hash()`)
   - ✅ Система ролей и прав доступа (RBAC)
   - ✅ Регенерация ID сессии после логина
   - ✅ Проверка прав доступа везде

5. **Security Headers**
   - ✅ Content-Security-Policy (CSP)
   - ✅ X-Frame-Options (защита от clickjacking)
   - ✅ X-Content-Type-Options (защита от MIME sniffing)
   - ✅ X-XSS-Protection
   - ✅ Referrer-Policy
   - ✅ Strict-Transport-Security (HSTS)
   - ✅ Permissions-Policy

6. **Файловая безопасность**
   - ✅ .htaccess защита для `.ini`, `.log`, `.sql` файлов
   - ✅ Защита директорий `engine/data/` и `storage/temp/`
   - ✅ Валидация загружаемых файлов (MIME типы, размеры)

### ⚠️ Рекомендации для реализации

1. **Rate Limiting**
   - [ ] Ограничение количества запросов с одного IP
   - [ ] Ограничение для API endpoints
   - [ ] Разные лимиты для разных действий
   - [ ] Хранение счетчиков в Redis/Memcached

2. **Защита от DDoS**
   - [ ] Cloudflare или другой CDN с защитой
   - [ ] Rate limiting на уровне веб-сервера
   - [ ] Blacklist для подозрительных IP

3. **Логирование безопасности**
   - [ ] Логирование всех попыток логина
   - [ ] Логирование подозрительной активности
   - [ ] Уведомления администратору о критичных событиях

4. **Двухфакторная аутентификация (2FA)**
   - [ ] TOTP (Time-based One-Time Password)
   - [ ] SMS верификация
   - [ ] Email верификация
   - [ ] Backup коды

5. **Аудит действий**
   - [ ] Логирование всех действий администратора
   - [ ] История изменений настроек
   - [ ] Отслеживание изменений в БД

6. **Шифрование данных**
   - [ ] Шифрование чувствительных данных в БД
   - [ ] Шифрование файлов в `storage/`
   - [ ] Использование HTTPS везде (принудительное)

---

## 🚀 Расширяемость через плагины и темы

### ✅ Реализовано

1. **Система плагинов**
   - ✅ Автоматическое обнаружение плагинов в `plugins/`
   - ✅ Базовый класс `BasePlugin` для разработки
   - ✅ Жизненный цикл: `init()`, `activate()`, `deactivate()`, `install()`, `uninstall()`
   - ✅ Хранение настроек плагинов в БД (`plugin_settings`)
   - ✅ Система nonces для безопасности
   - ✅ Интеграция с хуками системы

2. **Система тем**
   - ✅ Автоматическое обнаружение тем в `themes/`
   - ✅ Управление активной темой через БД
   - ✅ Хранение настроек тем (`theme_settings`)
   - ✅ Кастомизатор тем (ThemeCustomizer)
   - ✅ Редактор тем (ThemeEditor)
   - ✅ Поддержка SCSS компиляции

3. **Система хуков**
   - ✅ Фильтры для модификации данных
   - ✅ События для выполнения действий
   - ✅ Приоритизация хуков
   - ✅ Условное выполнение хуков
   - ✅ Стандартные хуки: `admin_menu`, `admin_routes`, `public_routes`, `dashboard_widgets`, `admin_footer`

### 📝 Рекомендации для разработчиков плагинов

#### Структура плагина

```
plugins/my-plugin/
├── plugin.php              # Главный файл плагина (обязателен)
├── plugin.json             # Метаданные плагина (обязателен)
├── includes/               # Дополнительные классы плагина
│   ├── MyPluginClass.php
│   └── Helpers.php
├── templates/              # Шаблоны плагина
│   ├── admin/
│   │   └── settings.php
│   └── public/
│       └── widget.php
├── assets/                 # Статические файлы
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── script.js
│   └── images/
│       └── icon.png
├── migrations/             # Миграции БД (будущее)
│   ├── 001_create_table.php
│   └── 002_add_column.php
└── languages/              # Переводы (будущее)
    ├── en.json
    └── uk.json
```

#### Базовый пример плагина

```php
<?php
/**
 * Plugin Name: My Plugin
 * Plugin Slug: my-plugin
 * Version: 1.0.0
 * Description: Описание плагина
 * Author: Your Name
 */

declare(strict_types=1);

class MyPlugin extends BasePlugin {
    
    public function init() {
        // Регистрация хуков
        addAction('admin_menu', [$this, 'addMenuItem']);
        addFilter('dashboard_widgets', [$this, 'addWidget']);
        addAction('admin_routes', [$this, 'registerRoutes']);
        
        // Загрузка CSS/JS
        addAction('admin_footer', [$this, 'enqueueAssets']);
    }
    
    public function activate() {
        // Создание таблиц, настройки и т.д.
        $this->createTables();
    }
    
    public function deactivate() {
        // Очистка временных данных
    }
    
    public function install() {
        // Полная установка (миграции, начальные данные)
        $this->runMigrations();
    }
    
    public function uninstall() {
        // Полное удаление (таблицы, настройки)
        $this->dropTables();
    }
    
    private function createTables() {
        $db = DatabaseHelper::getConnection();
        $db->exec("CREATE TABLE IF NOT EXISTS my_plugin_data (...)");
    }
}
```

### 📝 Рекомендации для разработчиков тем

#### Структура темы

```
themes/my-theme/
├── style.css              # Метаданные темы (обязателен)
├── functions.php          # Функции темы, хуки (обязателен)
├── index.php              # Главный шаблон (обязателен)
├── header.php             # Шапка сайта
├── footer.php             # Подвал сайта
├── sidebar.php            # Боковая панель (опционально)
├── assets/                # Статические файлы
│   ├── css/
│   │   ├── style.css
│   │   └── admin.css
│   ├── js/
│   │   └── theme.js
│   └── images/
├── templates/             # Дополнительные шаблоны
│   ├── page.php
│   ├── post.php
│   └── archive.php
├── customizer/            # Настройки кастомизатора
│   └── settings.php
└── languages/             # Переводы (будущее)
    ├── en.json
    └── uk.json
```

#### Базовый пример темы

```php
<?php
/**
 * Theme Name: My Theme
 * Theme Slug: my-theme
 * Version: 1.0.0
 * Description: Описание темы
 * Author: Your Name
 */

// functions.php

// Регистрация хуков
addAction('theme_before_content', function() {
    echo '<div class="container">';
});

addAction('theme_after_content', function() {
    echo '</div>';
});

// Регистрация маршрутов
addAction('public_routes', function($router) {
    $router->get('/about', function() {
        return View::make('about');
    });
});

// Регистрация настроек кастомизатора
addFilter('theme_customizer_settings', function($settings) {
    $settings[] = [
        'id' => 'primary_color',
        'label' => 'Основной цвет',
        'type' => 'color',
        'default' => '#0073aa'
    ];
    return $settings;
});
```

---

## 🛡️ Best Practices для безопасности

### 1. Разработка плагинов

#### ✅ Обязательно:
- Использовать prepared statements для всех SQL запросов
- Валидировать все входные данные через `Validator`
- Санитизировать вывод через `htmlspecialchars()` или `Security::clean()`
- Проверять CSRF токены во всех формах
- Проверять права доступа через `current_user_can()`
- Использовать nonces для действий плагина (`$this->createNonce()`, `$this->verifyNonce()`)
- Использовать префиксы для данных в сессии (`sessionManager('plugin_slug')`)
- Логировать все критичные действия

#### ❌ Никогда:
- Не используйте прямые SQL запросы без prepared statements
- Не доверяйте входным данным пользователя
- Не выводите данные без экранирования
- Не игнорируйте CSRF токены
- Не сохраняйте пароли в открытом виде
- Не используйте `eval()` или `exec()`

### 2. Разработка тем

#### ✅ Обязательно:
- Экранировать все выводы через `htmlspecialchars()`
- Использовать `SecurityHelper::sanitizeInput()` для всех входных данных
- Проверять права доступа перед отображением контента
- Использовать хелперы системы (`UrlHelper::admin()`, `SecurityHelper::csrfField()`)

### 3. Работа с БД

#### ✅ Обязательно:
```php
// ✅ ПРАВИЛЬНО - используем prepared statements
$stmt = $db->prepare("SELECT * FROM users WHERE id = ?");
$stmt->execute([$userId]);

// ❌ НЕПРАВИЛЬНО - уязвимо к SQL injection
$db->query("SELECT * FROM users WHERE id = " . $userId);
```

### 4. Работа с файлами

#### ✅ Обязательно:
```php
// ✅ ПРАВИЛЬНО - валидация MIME типа
$upload = new Upload('file_field');
$upload->validateMimeType(['image/jpeg', 'image/png']);
$upload->validateSize(5 * 1024 * 1024); // 5MB

// ❌ НЕПРАВИЛЬНО - без валидации
move_uploaded_file($_FILES['file']['tmp_name'], $path);
```

### 5. Работа с сессиями

#### ✅ Обязательно:
```php
// ✅ ПРАВИЛЬНО - используем SessionManager с префиксом
$session = sessionManager('my_plugin');
$session->set('key', 'value');

// ❌ НЕПРАВИЛЬНО - прямой доступ к $_SESSION
$_SESSION['key'] = 'value';
```

---

## 📚 Документация для разработчиков

### Существующая документация

1. **Система хуков:** `.dev/wiki/engine/classes/system/HOOKS_DOCUMENTATION.md`
2. **Менеджеры хранилища:** `.dev/docs/STORAGE_MANAGERS.md`
3. **Примеры плагинов:** `.dev/wiki/plugins/test-hooks-plugin-README.md`
4. **Примеры тем:** `.dev/wiki/themes/test-theme-README.md`

### Необходимая документация

1. **📘 Полное руководство по разработке плагинов** (приоритет: ВЫСОКИЙ)
   - Структура плагина
   - Жизненный цикл плагина
   - Работа с хуками
   - Работа с БД
   - Создание админ-страниц
   - Создание публичных страниц
   - Загрузка файлов
   - Примеры кода

2. **📗 Полное руководство по разработке тем** (приоритет: ВЫСОКИЙ)
   - Структура темы
   - Шаблоны тем
   - Регистрация маршрутов
   - Использование хуков
   - Кастомизатор тем
   - Работа с assets
   - Примеры кода

3. **📙 API Reference** (приоритет: СРЕДНИЙ)
   - Все классы и методы
   - Хелперы
   - Примеры использования
   - Автогенерация из PHPDoc

4. **📕 Security Guide** (приоритет: ВЫСОКИЙ)
   - Best practices безопасности
   - Защита от распространенных уязвимостей
   - Примеры безопасного кода
   - Чек-лист безопасности

---

## 🔧 Настройка для крупных проектов

### 1. Production настройки

#### PHP настройки (`php.ini`)
```ini
; Безопасность
display_errors = Off
expose_php = Off
allow_url_fopen = Off
allow_url_include = Off

; Производительность
opcache.enable = 1
opcache.memory_consumption = 128
opcache.max_accelerated_files = 10000
realpath_cache_size = 4M
realpath_cache_ttl = 3600

; Лимиты
max_execution_time = 30
memory_limit = 256M
upload_max_filesize = 10M
post_max_size = 12M
```

#### .htaccess улучшения
```apache
# Security Headers
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # HSTS (только для HTTPS)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    
    # CSP (можно настроить для каждой страницы)
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net;"
</IfModule>

# Защита от directory listing
Options -Indexes

# Защита файлов конфигурации
<FilesMatch "\.(ini|log|sql|bak)$">
    Order allow,deny
    Deny from all
</FilesMatch>
```

### 2. База данных

#### Оптимизация
- Использовать индексы для часто запрашиваемых колонок
- Регулярная оптимизация таблиц (`OPTIMIZE TABLE`)
- Регулярное резервное копирование
- Мониторинг медленных запросов

#### Безопасность
- Отдельный пользователь БД для приложения
- Минимальные права для пользователя БД (только SELECT, INSERT, UPDATE, DELETE)
- Регулярное обновление MySQL/MariaDB
- Шифрование соединения (SSL)

### 3. Мониторинг

#### Необходимые метрики
- [ ] Время ответа страниц
- [ ] Использование памяти
- [ ] Количество запросов к БД
- [ ] Размер кеша
- [ ] Количество ошибок в логах
- [ ] Активность пользователей
- [ ] Использование API

#### Инструменты
- [ ] Интеграция с Sentry для отслеживания ошибок
- [ ] Интеграция с LogRocket для отслеживания сессий
- [ ] Google Analytics для аналитики
- [ ] Мониторинг сервера (CPU, память, диск)

---

## 🎓 Примеры использования

### Создание простого плагина

**plugins/hello-world/plugin.php:**
```php
<?php
declare(strict_types=1);

class HelloWorldPlugin extends BasePlugin {
    
    public function init() {
        // Добавляем виджет на dashboard
        addFilter('dashboard_widgets', [$this, 'addWidget'], 10);
        
        // Добавляем пункт меню
        addAction('admin_menu', [$this, 'addMenuItem'], 20);
        
        // Регистрируем маршрут
        addAction('admin_routes', [$this, 'registerRoute'], 10);
    }
    
    public function addWidget($widgets) {
        $widgets[] = [
            'title' => 'Hello World',
            'content' => '<p>Это виджет от плагина Hello World!</p>',
            'order' => 10
        ];
        return $widgets;
    }
    
    public function addMenuItem($menu) {
        $menu[] = [
            'text' => 'Hello World',
            'icon' => 'fas fa-globe',
            'href' => UrlHelper::admin('hello-world'),
            'page' => 'hello-world',
            'order' => 50
        ];
        return $menu;
    }
    
    public function registerRoute($router) {
        $router->get('hello-world', 'HelloWorldPage');
    }
}
```

### Создание простой темы

**themes/my-theme/style.css:**
```css
/*
Theme Name: My Theme
Theme Slug: my-theme
Version: 1.0.0
Description: Простая тема для Flowaxy CMS
Author: Your Name
*/

/* CSS стили темы */
```

**themes/my-theme/functions.php:**
```php
<?php
// Регистрация публичных маршрутов
addAction('public_routes', function($router) {
    $router->get('/', function() {
        return View::make('home');
    });
    
    $router->get('/about', function() {
        return View::make('about');
    });
});

// Использование хуков
addAction('theme_before_header', function() {
    echo '<!-- Before header -->';
});

addAction('theme_after_footer', function() {
    echo '<!-- After footer -->';
});
```

---

## 📌 Приоритеты для крупного проекта

### Фаза 1: Безопасность и стабильность (КРИТИЧНО)
1. ✅ Security Headers
2. ⚠️ Rate Limiting для API
3. ⚠️ Логирование безопасности
4. ⚠️ Двухфакторная аутентификация
5. ⚠️ Аудит действий администратора

### Фаза 2: Документация для разработчиков (ВЫСОКИЙ приоритет)
1. ⚠️ Полное руководство по разработке плагинов
2. ⚠️ Полное руководство по разработке тем
3. ⚠️ Security Guide для разработчиков
4. ⚠️ API Reference с примерами

### Фаза 3: Расширяемость (СРЕДНИЙ приоритет)
1. ⚠️ Система миграций для плагинов
2. ⚠️ Улучшенная система хуков (еще больше стандартных хуков)
3. ⚠️ Система событий для плагинов (plugin lifecycle events)

### Фаза 4: Производительность (СРЕДНИЙ приоритет)
1. ⚠️ Кеширование запросов к БД
2. ⚠️ Оптимизация запросов
3. ⚠️ Минификация CSS/JS для продакшена
4. ⚠️ CDN интеграция

### Фаза 5: Мониторинг и аналитика (НИЗКИЙ приоритет)
1. ⚠️ Интеграция с Sentry
2. ⚠️ Интеграция с аналитикой
3. ⚠️ Dashboard с метриками производительности

---

## 🔄 История изменений

### Версия 7.0.0 (текущая) - 2025-01-21
- ✅ **Система менеджеров хранилища**
  - CookieManager - управление cookies
  - SessionManager - управление сессиями с префиксами
  - StorageManager - управление LocalStorage/SessionStorage
  - StorageFactory - фабрика для создания менеджеров
  - JavaScript API для клиентского хранилища
  - Все файлы системы обновлены для использования менеджеров
- ✅ **RouterManager**
  - Переименован из CentralRouter
  - Перенесен в `engine/classes/managers/`
  - Улучшена логика определения контекста
  - Блокировка установщика после установки
  - Сохранение настроек БД в сессии во время установки
- ✅ **Security Headers**
  - Добавлены в класс Response
  - Автоматическая установка при инициализации
  - CSP, X-Frame-Options, X-Content-Type-Options, HSTS
- ✅ **Упрощенная система ролей**
  - Только базовые роли: Guest, User, Developer
  - Роли хранятся в JSON колонке `role_ids` таблицы `users`
  - Удалена таблица `user_roles`
  - Удалены все `cabinet.*` разрешения (будут в плагине)

### Версия 6.0.0
- Базовая архитектура системы
- Административная панель
- Система плагинов и тем
- Базовые компоненты ядра
- ✅ **Полноценная система хуков и событий** (HookManager)
- ✅ **Перенос функциональности меню в плагин**
- ✅ **API для внешних приложений и Webhooks**

---

## 🐛 Известные проблемы

_Здесь будут отмечаться найденные баги и проблемы_

---

## 💡 Идеи для будущего

_Здесь будут записываться идеи для будущих версий_

---

**Последнее обновление:** 2025-01-21  
**Статус:** ✅ Готова к использованию в крупных проектах  
**Возможность расширения:** ✅ Полная (только через плагины и темы)
