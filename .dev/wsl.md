# Полная установка Flowaxy CMS на WSL2

**Пошаговая инструкция от А до Я для установки dev.flowaxy.com**

**Домен:** `dev.flowaxy.com`  
**Путь проекта:** `D:\projects\domains\flowaxy.com\dev`  
**База данных:** MySQL, пользователь `root`, пароль `#Nt2GDaUm4r5rp8i`  
**Удаленное подключение:** HeidiSQL через порт 3306

---

## 📋 Быстрая настройка (Автоматический скрипт)

Если вы хотите быстро установить все, создайте и выполните автоматический скрипт:

### Создание скрипта автоматической установки

```bash
nano ~/install-flowaxy.sh
```

Вставьте содержимое из раздела [Автоматическая установка](#автоматическая-установка) ниже.

Сделайте исполняемым и запустите:

```bash
chmod +x ~/install-flowaxy.sh
~/install-flowaxy.sh
```

---

## Содержание

1. [Включение WSL2](#1-включение-wsl2)
2. [Настройка Ubuntu](#2-настройка-ubuntu)
3. [Установка PHP 8.4](#3-установка-php-84)
4. [Установка и настройка MySQL](#4-установка-и-настройка-mysql)
5. [Настройка удаленного доступа MySQL для HeidiSQL](#5-настройка-удаленного-доступа-mysql-для-heidisql)
6. [Установка Apache с поддержкой .htaccess](#6-установка-apache-с-поддержкой-htaccess)
7. [Настройка виртуального хоста dev.flowaxy.com](#7-настройка-виртуального-хоста-devflowaxycom)
8. [Настройка hosts файла](#8-настройка-hosts-файла)
9. [Настройка прав доступа и структуры проекта](#9-настройка-прав-доступа-и-структуры-проекта)
10. [Подключение VS Code](#10-подключение-vs-code)
11. [Автозапуск сервисов](#11-автозапуск-сервисов)
12. [Настройка портов и сетевого доступа](#12-настройка-портов-и-сетевого-доступа)
13. [Настройка SSL с Let's Encrypt](#13-настройка-ssl-с-lets-encrypt)
14. [Установка Flowaxy CMS](#14-установка-flowaxy-cms)
15. [Полезные команды](#15-полезные-команды)
16. [Полная очистка и переустановка WSL](#16-полная-очистка-и-переустановка-wsl)

---

## 1. Включение WSL2

### Шаг 1: Установка WSL2

Откройте **PowerShell от имени администратора** и выполните:

```powershell
wsl --install
```

Эта команда установит:
- WSL2
- Ubuntu (по умолчанию)
- Виртуальную машину (Virtual Machine Platform)

### Шаг 2: Перезагрузка

После установки **обязательно перезагрузите компьютер**.

### Шаг 3: Первый запуск

После перезагрузки откройте Ubuntu из меню Пуск. При первом запуске:
1. Введите имя пользователя (латиницей, без пробелов)
2. Введите пароль (символы не будут отображаться при вводе)
3. Подтвердите пароль

### Проверка версии WSL

```bash
wsl --version
```

Должна быть версия 2.x или выше.

---

## 2. Настройка Ubuntu

### Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

### Установка необходимых утилит

```bash
sudo apt install -y curl wget git unzip nano
```

---

## 3. Установка PHP 8.4

### Шаг 1: Добавление репозитория PHP

```bash
sudo apt install -y software-properties-common
sudo add-apt-repository ppa:ondrej/php -y
sudo apt update
```

### Шаг 2: Установка PHP и расширений

```bash
sudo apt install -y php8.4 \
    php8.4-fpm \
    php8.4-cli \
    php8.4-mysql \
    php8.4-xml \
    php8.4-curl \
    php8.4-zip \
    php8.4-gd \
    php8.4-intl \
    php8.4-bcmath \
    php8.4-mbstring \
    php8.4-opcache
```

### Шаг 3: Проверка установки

```bash
php -v
```

Должна отобразиться версия PHP 8.4.x

### Шаг 4: Настройка PHP для Apache

Модуль PHP активируется автоматически при установке `libapache2-mod-php8.4`. Проверьте, что PHP работает с Apache:

```bash
# Проверка, что модуль PHP загружен в Apache
apache2ctl -M | grep php

# Или
apache2ctl -M | grep -i php

# Если модуль не найден, проверьте установку
dpkg -l | grep libapache2-mod-php

# Если пакет не установлен, установите его
sudo apt install libapache2-mod-php8.4 -y

# После установки перезапустите Apache
sudo systemctl restart apache2

# Проверка версии PHP через Apache
php -v
```

**Примечание:** Если команда `apache2ctl` не найдена, убедитесь, что Apache установлен:
```bash
# Проверка установки Apache
which apache2
dpkg -l | grep apache2

# Если Apache не установлен, установите его (см. раздел 6)
sudo apt install apache2 -y
```

### Шаг 5: Настройка php.ini (опционально)

Откройте конфигурацию PHP:

```bash
sudo nano /etc/php/8.4/apache2/php.ini
```

Основные настройки (найдите и раскомментируйте/измените):

```ini
upload_max_filesize = 64M
post_max_size = 64M
memory_limit = 256M
date.timezone = Europe/Kiev
display_errors = Off
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
```

После изменения:

```bash
sudo systemctl restart apache2
```

---

## 4. Установка и настройка MySQL

### Шаг 1: Установка MySQL

```bash
sudo apt install mysql-server -y
```

### Шаг 2: Автоматическая настройка MySQL с паролем root

Создайте скрипт для автоматической настройки MySQL:

```bash
sudo nano /tmp/mysql-setup.sh
```

Вставьте следующий скрипт:

```bash
#!/bin/bash
# Автоматическая настройка MySQL для dev.flowaxy.com

MYSQL_ROOT_PASSWORD="#Nt2GDaUm4r5rp8i"
MYSQL_DATABASE="flowaxy_dev"

# Запуск MySQL сервера
sudo systemctl start mysql
sleep 3

# Настройка пароля root и создание базы данных
sudo mysql <<EOF
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '$MYSQL_ROOT_PASSWORD';
FLUSH PRIVILEGES;

# Создание базы данных для проекта
CREATE DATABASE IF NOT EXISTS \`$MYSQL_DATABASE\` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# Разрешение удаленного доступа для root (только для разработки)
CREATE USER IF NOT EXISTS 'root'@'%' IDENTIFIED BY '$MYSQL_ROOT_PASSWORD';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;

FLUSH PRIVILEGES;
EXIT;
EOF

echo "MySQL настроен успешно!"
echo "База данных: $MYSQL_DATABASE"
echo "Пользователь: root"
echo "Пароль: $MYSQL_ROOT_PASSWORD"
```

Сделайте исполняемым и запустите:

```bash
chmod +x /tmp/mysql-setup.sh
sudo bash /tmp/mysql-setup.sh
```

### Шаг 3: Проверка подключения к MySQL

```bash
# Подключение к MySQL с паролем
mysql -u root -p'#Nt2GDaUm4r5rp8i' -e "SHOW DATABASES;"

# Проверка создания базы данных
mysql -u root -p'#Nt2GDaUm4r5rp8i' -e "SHOW DATABASES LIKE 'flowaxy_dev';"
```

### Шаг 4: Включение автозапуска MySQL

```bash
sudo systemctl enable mysql
sudo systemctl status mysql
```

### Шаг 5: Проверка работы MySQL

```bash
# Статус MySQL
sudo systemctl status mysql

# Проверка подключения
mysql -u root -p'#Nt2GDaUm4r5rp8i' -e "SELECT VERSION();"
```

---

## 5. Настройка удаленного доступа MySQL для HeidiSQL

### Шаг 1: Настройка MySQL для удаленных подключений

Откройте конфигурацию MySQL:

```bash
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf
```

Найдите строку `bind-address`:

```ini
bind-address = 127.0.0.1
```

Измените на:

```ini
bind-address = 0.0.0.0
```

**Важно:** `bind-address = 0.0.0.0` позволяет подключаться извне. Для продакшена рекомендуется ограничить доступ по IP.

### Шаг 2: Перезапуск MySQL

```bash
sudo systemctl restart mysql
```

### Шаг 3: Проверка открытости порта MySQL

```bash
# Проверка, что MySQL слушает на всех интерфейсах
sudo netstat -tulpn | grep :3306
```

Должно быть что-то вроде: `0.0.0.0:3306` или `:::3306`

### Шаг 4: Настройка доступа в MySQL (если еще не сделано)

Если пользователь root@'%' еще не создан:

```bash
mysql -u root -p'#Nt2GDaUm4r5rp8i' <<EOF
CREATE USER IF NOT EXISTS 'root'@'%' IDENTIFIED BY '#Nt2GDaUm4r5rp8i';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' WITH GRANT OPTION;
FLUSH PRIVILEGES;
EXIT;
EOF
```

### Шаг 5: Определение IP адресов для подключения

Узнайте IP адрес Windows хоста и WSL:

```bash
# IP адрес WSL
hostname -I | awk '{print $1}'

# IP адрес Windows хоста
cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
```

### Шаг 6: Настройка Windows Firewall для MySQL

Откройте **PowerShell от имени администратора**:

```powershell
# Разрешить MySQL порт (3306)
netsh advfirewall firewall add rule name="WSL MySQL Remote" dir=in action=allow protocol=TCP localport=3306

# Проверка правила
netsh advfirewall firewall show rule name="WSL MySQL Remote"
```

### Шаг 7: Настройка проброса порта MySQL (опционально)

Если нужно подключиться через внешний IP:

```powershell
# В PowerShell от имени администратора
# Узнайте IP WSL (из WSL: hostname -I)
$WSL_IP = "172.x.x.x"  # Замените на реальный IP WSL

# Проброс MySQL порта (3306)
netsh interface portproxy add v4tov4 listenport=3306 listenaddress=0.0.0.0 connectport=3306 connectaddress=$WSL_IP

# Проверка проброса
netsh interface portproxy show all
```

### Шаг 8: Подключение через HeidiSQL

**Настройки подключения в HeidiSQL:**

1. **Имя подключения:** `dev.flowaxy.com`
2. **Тип сети:** `MySQL (TCP/IP)`
3. **Хост/IP:** `127.0.0.1` (локально) или IP адрес Windows хоста (из WSL)
4. **Порт:** `3306`
5. **Пользователь:** `root`
6. **Пароль:** `#Nt2GDaUm4r5rp8i`
7. **База данных:** `flowaxy_dev` (опционально, можно выбрать после подключения)

**Для подключения из интернета:**

Если настроен проброс портов на роутере:
- **Хост/IP:** Ваш внешний IP адрес (узнать: `curl ifconfig.me`)
- **Порт:** `3306`
- **Пользователь:** `root`
- **Пароль:** `#Nt2GDaUm4r5rp8i`

### Шаг 9: Проверка подключения через HeidiSQL

1. Откройте HeidiSQL
2. Создайте новое подключение с указанными параметрами
3. Нажмите "Открыть"
4. Если подключение успешно, вы увидите список баз данных

### Шаг 10: Создание базы данных через HeidiSQL (если нужно)

Если база данных еще не создана, выполните в HeidiSQL:

```sql
CREATE DATABASE IF NOT EXISTS `flowaxy_dev` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Шаг 11: Безопасность (рекомендации)

⚠️ **Важно для продакшена:**

1. **Не используйте root для подключений** - создайте отдельного пользователя:

```sql
CREATE USER 'flowaxy_user'@'%' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON flowaxy_dev.* TO 'flowaxy_user'@'%';
FLUSH PRIVILEGES;
```

2. **Ограничьте доступ по IP** - разрешите подключения только с определенных IP:

```sql
CREATE USER 'flowaxy_user'@'192.168.1.100' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON flowaxy_dev.* TO 'flowaxy_user'@'192.168.1.100';
FLUSH PRIVILEGES;
```

3. **Используйте firewall** для ограничения доступа к порту 3306 только с необходимых IP

---

---

## 6. Установка Apache с поддержкой .htaccess

### Шаг 1: Установка Apache и модуля PHP

```bash
# Установка Apache
sudo apt install apache2 -y

# Установка модуля PHP для Apache (активируется автоматически)
sudo apt install libapache2-mod-php8.4 -y

# Проверка, что модуль PHP активирован
apache2ctl -M | grep php

# Если модуль не найден, проверьте установку
dpkg -l | grep libapache2-mod-php8.4
```

**Важно:** Модуль PHP активируется автоматически при установке пакета `libapache2-mod-php8.4`. Команда `a2enmod php8.4` **не нужна** и не существует, так как PHP не является модулем Apache в традиционном смысле.

### Шаг 2: Включение необходимых модулей

```bash
sudo a2enmod rewrite
sudo a2enmod headers
sudo a2enmod expires
sudo a2enmod deflate
```

### Шаг 3: Настройка AllowOverride

Откройте конфигурацию Apache:

```bash
sudo nano /etc/apache2/apache2.conf
```

Найдите блок:

```apache
<Directory /var/www/>
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>
```

Измените на:

```apache
<Directory /var/www/>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
```

**Для проекта dev.flowaxy.com**, добавьте отдельный блок:

```apache
<Directory /mnt/d/projects/domains/flowaxy.com/dev>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
```

### Шаг 4: Перезапуск Apache

```bash
sudo systemctl restart apache2
```

### Шаг 5: Проверка работы Apache

Откройте в браузере: `http://localhost`

Должна отобразиться страница "Apache2 Ubuntu Default Page"

---

## 7. Настройка виртуального хоста dev.flowaxy.com

### Шаг 1: Создание конфигурации сайта для dev.flowaxy.com

```bash
sudo nano /etc/apache2/sites-available/dev.flowaxy.conf
```

### Шаг 2: Конфигурация для dev.flowaxy.com

Вставьте следующую конфигурацию (замените пути на свои, если они отличаются):

```apache
<VirtualHost *:80>
    ServerName dev.flowaxy.com
    ServerAlias www.dev.flowaxy.com
    DocumentRoot /mnt/d/projects/domains/flowaxy.com/dev
    
    <Directory /mnt/d/projects/domains/flowaxy.com/dev>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
        
        # Улучшенная безопасность
        <FilesMatch "\.(htaccess|htpasswd|ini|log|sql|bak)$">
            Require all denied
        </FilesMatch>
    </Directory>
    
    # Логи сайта
    ErrorLog ${APACHE_LOG_DIR}/dev.flowaxy_error.log
    CustomLog ${APACHE_LOG_DIR}/dev.flowaxy_access.log combined
    
    # Обработка PHP файлов
    <FilesMatch \.php$>
        SetHandler application/x-httpd-php
    </FilesMatch>
    
    # Дополнительные настройки безопасности
    <IfModule mod_headers.c>
        # Удаляем информацию о сервере
        Header unset X-Powered-By
        Header unset Server
        
        # Security Headers (можно настроить более строгие)
        Header always set X-Content-Type-Options "nosniff"
        Header always set X-Frame-Options "SAMEORIGIN"
        Header always set X-XSS-Protection "1; mode=block"
        Header always set Referrer-Policy "strict-origin-when-cross-origin"
    </IfModule>
    
    # Кеширование статических файлов
    <IfModule mod_expires.c>
        ExpiresActive On
        ExpiresByType image/jpg "access plus 1 year"
        ExpiresByType image/jpeg "access plus 1 year"
        ExpiresByType image/gif "access plus 1 year"
        ExpiresByType image/png "access plus 1 year"
        ExpiresByType image/svg+xml "access plus 1 year"
        ExpiresByType text/css "access plus 1 month"
        ExpiresByType application/javascript "access plus 1 month"
        ExpiresByType font/woff "access plus 1 year"
        ExpiresByType font/woff2 "access plus 1 year"
    </IfModule>
</VirtualHost>
```

### Шаг 3: Активация сайта

```bash
# Активация конфигурации
sudo a2ensite dev.flowaxy.conf

# Проверка конфигурации на ошибки
sudo apache2ctl configtest

# Если проверка прошла успешно, перезагрузите Apache
sudo systemctl reload apache2
```

**Примечание:** Если вы получили ошибку, что файл не найден, убедитесь, что используете правильное имя файла без расширения `.conf` в команде `a2ensite`:
```bash
sudo a2ensite dev.flowaxy
```

### Шаг 4: Отключение дефолтного сайта (опционально)

```bash
sudo a2dissite 000-default.conf
sudo systemctl reload apache2
```

---

## 8. Настройка hosts файла

### Шаг 1: Настройка hosts в Windows

Откройте **Notepad от имени администратора** и откройте файл:

```
C:\Windows\System32\drivers\etc\hosts
```

Добавьте строки для `dev.flowaxy.com`:

```
127.0.0.1    dev.flowaxy.com
127.0.0.1    www.dev.flowaxy.com
```

**Альтернативный вариант:** Если вы хотите также использовать локальный домен `flowaxy.local`, добавьте обе строки:

```
127.0.0.1    flowaxy.local
127.0.0.1    www.flowaxy.local
127.0.0.1    dev.flowaxy.com
127.0.0.1    www.dev.flowaxy.com
```

Сохраните файл (Ctrl+S).

### Шаг 2: Настройка hosts в WSL (опционально)

Если вы хотите обращаться к сайту из WSL по домену:

```bash
sudo nano /etc/hosts
```

Добавьте:

```
127.0.0.1    dev.flowaxy.com
127.0.0.1    www.dev.flowaxy.com
```

---

## 9. Настройка прав доступа и структуры проекта

### Шаг 1: Проверка работы виртуального хоста

1. **Из Windows браузера:** Откройте `http://dev.flowaxy.com`
2. **Из WSL:** Можно протестировать через curl:
   ```bash
   curl -I http://dev.flowaxy.com
   ```

Должен вернуться HTTP статус 200 и заголовки ответа.

### Шаг 2: Проверка логов (если что-то не работает)

```bash
# Просмотр ошибок Apache
sudo tail -f /var/log/apache2/error.log

# Просмотр логов конкретного сайта
sudo tail -f /var/log/apache2/dev.flowaxy_error.log

# Просмотр access логов
sudo tail -f /var/log/apache2/dev.flowaxy_access.log
```

---

## 8. Настройка hosts файла

### Шаг 1: Настройка hosts в Windows

Откройте **Notepad от имени администратора** и откройте файл:

```
C:\Windows\System32\drivers\etc\hosts
```

Добавьте строки для `dev.flowaxy.com`:

```
127.0.0.1    dev.flowaxy.com
127.0.0.1    www.dev.flowaxy.com
```

Сохраните файл (Ctrl+S).

### Шаг 2: Настройка hosts в WSL (опционально)

Если вы хотите обращаться к сайту из WSL по домену:

```bash
sudo nano /etc/hosts
```

Добавьте:

```
127.0.0.1    dev.flowaxy.com
127.0.0.1    www.dev.flowaxy.com
```

---

## 9. Настройка прав доступа и структуры проекта

### Пути файлов

- **В Windows**: `D:\projects\domains\flowaxy.com\dev`
- **В WSL**: `/mnt/d/projects/domains/flowaxy.com/dev`
- **Домен**: `dev.flowaxy.com` (или `http://dev.flowaxy.com` в браузере)
- **База данных**: MySQL, пользователь `root`, пароль `#Nt2GDaUm4r5rp8i`

### Важные замечания

⚠️ **Внимание**: Файлы на дисках Windows (`/mnt/`) могут работать медленнее. Для лучшей производительности:

1. **Перенесите проект в WSL** (рекомендуется):

```bash
# Создайте папку в домашней директории WSL
mkdir -p ~/projects
# Если нужно перенести проект
# mv /mnt/d/projects/domains/flowaxy.com/dev ~/projects/
```

2. **Или используйте WSL2 файловую систему** для кэша/временных файлов:

```bash
# Создайте симлинк для кэша (если нужно)
ln -s ~/cache /mnt/d/projects/domains/flowaxy.com/dev/storage/cache
```

### Настройка прав доступа

Для работы `.htaccess` и записи файлов:

```bash
# Дайте права на запись для папок, которые требуют записи
sudo chmod -R 775 /mnt/d/projects/domains/flowaxy.com/dev/storage
sudo chmod -R 775 /mnt/d/projects/domains/flowaxy.com/dev/uploads
sudo chmod -R 775 /mnt/d/projects/domains/flowaxy.com/dev/engine/data
```

---

## 9. Подключение VS Code

### Шаг 1: Установка расширения

В VS Code установите расширение: **Remote - WSL**

### Шаг 2: Открытие проекта в WSL

1. Откройте терминал WSL
2. Перейдите в папку проекта:

```bash
cd /mnt/d/projects/domains/flowaxy.com/dev
```

3. Откройте в VS Code:

```bash
code .
```

VS Code автоматически подключится к WSL и все расширения будут работать в контексте WSL.

### Полезные расширения для PHP

- PHP Intelephense
- PHP Debug
- PHP DocBlocker
- GitLens

---

## 10. Автозапуск сервисов

### Метод 1: Через wsl.conf (рекомендуется)

Создайте/отредактируйте файл:

```bash
sudo nano /etc/wsl.conf
```

Добавьте:

```ini
[boot]
command="service mysql start && service apache2 start"
```

После изменения закройте все терминалы WSL и выполните в PowerShell:

```powershell
wsl --shutdown
```

При следующем запуске WSL сервисы запустятся автоматически.

### Метод 2: Через systemd (альтернатива)

В более новых версиях WSL можно использовать systemd:

```bash
sudo nano /etc/wsl.conf
```

Добавьте:

```ini
[boot]
systemd=true
```

После этого можно управлять сервисами через systemctl:

```bash
sudo systemctl enable mysql
sudo systemctl enable apache2
```

---

## 11. Настройка портов и сетевого доступа

> ⚠️ **Важно**: Для работы SSL сертификатов и доступа к сайту из интернета необходимо открыть порты на WSL, Windows Firewall и настроить проброс портов на роутере.

### Шаг 1: Определение IP адреса WSL

Узнайте IP адрес WSL дистрибутива:

```bash
# В WSL терминале
hostname -I
# или
ip addr show eth0 | grep inet
```

Этот IP будет использоваться для проброса портов.

### Шаг 2: Получение IP адреса Windows хоста

Узнайте IP адрес Windows хоста из WSL:

```bash
# IP адрес Windows хоста из WSL
cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
```

Или в PowerShell Windows:

```powershell
ipconfig | findstr "IPv4"
```

### Шаг 3: Настройка Windows Firewall

Откройте **PowerShell от имени администратора** и выполните:

#### Открытие портов для Apache

```powershell
# Разрешить HTTP (порт 80)
netsh advfirewall firewall add rule name="WSL Apache HTTP" dir=in action=allow protocol=TCP localport=80

# Разрешить HTTPS (порт 443)
netsh advfirewall firewall add rule name="WSL Apache HTTPS" dir=in action=allow protocol=TCP localport=443

# Разрешить MySQL (порт 3306) - опционально, для внешнего доступа
netsh advfirewall firewall add rule name="WSL MySQL" dir=in action=allow protocol=TCP localport=3306

# Проверка правил
netsh advfirewall firewall show rule name="WSL Apache HTTP"
netsh advfirewall firewall show rule name="WSL Apache HTTPS"
```

#### Удаление правил (если нужно)

```powershell
netsh advfirewall firewall delete rule name="WSL Apache HTTP"
netsh advfirewall firewall delete rule name="WSL Apache HTTPS"
```

### Шаг 4: Настройка проброса портов через Windows (netsh portproxy)

Для доступа к WSL из интернета нужно пробросить порты от Windows к WSL:

```powershell
# В PowerShell от имени администратора

# Узнайте IP адрес WSL (из WSL терминала: hostname -I)
$WSL_IP = "172.x.x.x"  # Замените на реальный IP WSL

# Проброс HTTP порта (80)
netsh interface portproxy add v4tov4 listenport=80 listenaddress=0.0.0.0 connectport=80 connectaddress=$WSL_IP

# Проброс HTTPS порта (443)
netsh interface portproxy add v4tov4 listenport=443 listenaddress=0.0.0.0 connectport=443 connectaddress=$WSL_IP

# Проброс MySQL порта (3306) - опционально
netsh interface portproxy add v4tov4 listenport=3306 listenaddress=0.0.0.0 connectport=3306 connectaddress=$WSL_IP
```

#### Проверка проброшенных портов

```powershell
# Просмотр всех проброшенных портов
netsh interface portproxy show all

# Удаление проброса порта (если нужно)
netsh interface portproxy delete v4tov4 listenport=80 listenaddress=0.0.0.0
netsh interface portproxy delete v4tov4 listenport=443 listenaddress=0.0.0.0
```

#### Автоматический скрипт для проброса портов

Создайте скрипт для автоматического проброса портов (IP WSL может изменяться при перезагрузке):

**Создайте файл в WSL:**

```bash
nano ~/port-forward.sh
```

Вставьте:

```bash
#!/bin/bash
# Скрипт для автоматического проброса портов WSL в Windows

WSL_IP=$(hostname -I | awk '{print $1}')

if [ -z "$WSL_IP" ]; then
    echo "ERROR: Не удалось определить IP адрес WSL"
    exit 1
fi

echo "WSL IP: $WSL_IP"

# Выполняем команды в Windows PowerShell
powershell.exe -Command "
    # Удаляем старые правила
    netsh interface portproxy delete v4tov4 listenport=80 listenaddress=0.0.0.0 2>&1 | Out-Null
    netsh interface portproxy delete v4tov4 listenport=443 listenaddress=0.0.0.0 2>&1 | Out-Null
    
    # Добавляем новые правила
    netsh interface portproxy add v4tov4 listenport=80 listenaddress=0.0.0.0 connectport=80 connectaddress=$WSL_IP
    netsh interface portproxy add v4tov4 listenport=443 listenaddress=0.0.0.0 connectport=443 connectaddress=$WSL_IP
    
    Write-Host 'Порты успешно проброшены:'
    netsh interface portproxy show all
"
```

Сделайте исполняемым:

```bash
chmod +x ~/port-forward.sh
```

Добавьте в `~/.bashrc` или `~/.zshrc` для автоматического запуска:

```bash
echo "~/port-forward.sh" >> ~/.bashrc
```

### Шаг 5: Настройка роутера (Port Forwarding)

Для доступа к сайту из интернета необходимо настроить проброс портов на роутере:

#### Определение внешнего IP адреса

Узнайте внешний IP адрес вашего интернет-соединения:

```bash
# В браузере или через curl
curl ifconfig.me
# или
curl ipinfo.io/ip
```

#### Настройка проброса портов на роутере

1. **Войдите в панель управления роутером:**
   - Обычно: `http://192.168.1.1` или `http://192.168.0.1`
   - Проверьте инструкцию к роутеру

2. **Найдите раздел "Port Forwarding" или "Виртуальные серверы":**
   - D-Link: Advanced → Port Forwarding
   - TP-Link: Forwarding → Virtual Servers
   - ASUS: WAN → Virtual Server / Port Forwarding
   - Netgear: Advanced → Port Forwarding / Port Triggering
   - Zyxel: Network → NAT → Port Forwarding

3. **Добавьте правила проброса портов:**

   **HTTP (порт 80):**
   - Название: `HTTP-WSL`
   - Внешний порт: `80`
   - Внутренний порт: `80`
   - Внутренний IP: `192.168.x.x` (IP адрес вашего Windows компьютера)
   - Протокол: `TCP`
   - Включить: `Да`

   **HTTPS (порт 443):**
   - Название: `HTTPS-WSL`
   - Внешний порт: `443`
   - Внутренний порт: `443`
   - Внутренний IP: `192.168.x.x` (IP адрес вашего Windows компьютера)
   - Протокол: `TCP`
   - Включить: `Да`

   **MySQL (порт 3306) - опционально:**
   - Название: `MySQL-WSL`
   - Внешний порт: `3306`
   - Внутренний порт: `3306`
   - Внутренний IP: `192.168.x.x`
   - Протокол: `TCP`
   - Включить: `Да` (только если нужно внешнее подключение к БД)

4. **Сохраните настройки** и перезагрузите роутер (если требуется)

#### Определение внутреннего IP адреса Windows

Узнайте IP адрес вашего Windows компьютера в локальной сети:

**В PowerShell:**
```powershell
ipconfig | findstr "IPv4"
```

**В WSL:**
```bash
# IP адрес Windows хоста в локальной сети
powershell.exe -Command "(Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -like '192.168.*'}).IPAddress"
```

### Шаг 6: Настройка динамического DNS (опционально)

Если у вас динамический IP адрес, настройте DDNS для постоянного доступа:

#### Бесплатные DDNS сервисы:

1. **No-IP** (https://www.noip.com/)
   - Регистрация бесплатна
   - Поддержка автоматического обновления
   - Поддомены вида: `yourname.ddns.net`

2. **DuckDNS** (https://www.duckdns.org/)
   - Полностью бесплатный
   - Простая настройка
   - Поддомены вида: `yourname.duckdns.org`

3. **Dynu** (https://www.dynu.com/)
   - Бесплатные домены
   - Поддержка множества доменов

#### Настройка DDNS через скрипт

Создайте скрипт для автоматического обновления IP:

```bash
nano ~/update-ddns.sh
```

Пример для DuckDNS:

```bash
#!/bin/bash
# Скрипт для обновления DuckDNS

DOMAIN="yourname"
TOKEN="your-token-here"

URL="https://www.duckdns.org/update?domains=$DOMAIN&token=$TOKEN&ip="

curl -s $URL

echo "DuckDNS updated for $DOMAIN.duckdns.org"
```

Сделайте исполняемым и добавьте в cron:

```bash
chmod +x ~/update-ddns.sh

# Добавить в cron для обновления каждые 5 минут
crontab -e
# Добавить строку:
# */5 * * * * /home/username/update-ddns.sh
```

### Шаг 7: Проверка доступности портов

#### Проверка из локальной сети

```bash
# Проверка HTTP (порт 80)
curl -I http://your-local-ip

# Проверка HTTPS (порт 443)
curl -I https://your-local-ip
```

#### Проверка из интернета

Используйте онлайн-сервисы для проверки:

- **Can You See Me** (https://canyouseeme.org/)
  - Введите порт (80 или 443)
  - Нажмите "Check Port"
  - Если порт открыт, увидите сообщение "Success"

- **Port Checker** (https://www.yougetsignal.com/tools/open-ports/)
  - Введите ваш внешний IP
  - Выберите порт (80 или 443)
  - Проверьте статус

#### Проверка через telnet

```bash
# Проверка HTTP порта
telnet your-external-ip 80

# Проверка HTTPS порта
telnet your-external-ip 443
```

### Шаг 8: Настройка статического IP для Windows (рекомендуется)

Для стабильной работы проброса портов рекомендуется настроить статический IP для Windows компьютера:

#### Через настройки Windows:

1. Откройте **Панель управления** → **Сеть и Интернет** → **Центр управления сетями и общим доступом**
2. Нажмите на ваше подключение (Ethernet или Wi-Fi)
3. Нажмите **Свойства** → **Протокол Интернета версии 4 (TCP/IPv4)** → **Свойства**
4. Выберите **Использовать следующий IP-адрес:**
   - IP-адрес: `192.168.x.100` (замените x на ваш подсеть, используйте IP вне диапазона DHCP)
   - Маска подсети: `255.255.255.0`
   - Основной шлюз: `192.168.x.1` (IP вашего роутера)
   - Предпочитаемый DNS-сервер: `8.8.8.8` (Google DNS) или `1.1.1.1` (Cloudflare DNS)
   - Альтернативный DNS-сервер: `8.8.4.4` или `1.0.0.1`
5. Нажмите **ОК** и закройте все окна

### Шаг 9: Решение проблем

#### Проблема: Порты не доступны из интернета

1. **Проверьте Windows Firewall:**
   ```powershell
   netsh advfirewall firewall show rule name="WSL Apache HTTP"
   netsh advfirewall firewall show rule name="WSL Apache HTTPS"
   ```

2. **Проверьте проброс портов:**
   ```powershell
   netsh interface portproxy show all
   ```

3. **Проверьте настройки роутера:**
   - Убедитесь, что правила проброса портов активны
   - Проверьте, что указан правильный внутренний IP

4. **Проверьте, что Apache слушает правильные интерфейсы:**
   ```bash
   sudo netstat -tulpn | grep :80
   sudo netstat -tulpn | grep :443
   ```

#### Проблема: WSL IP адрес изменяется при перезагрузке

Используйте скрипт автоматического проброса портов (см. Шаг 4) или настройте статический IP для WSL:

```bash
sudo nano /etc/netplan/50-wsl.yaml
```

Вставьте (замените IP на подходящий):

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 172.20.0.2/20
      gateway4: 172.20.0.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

Примените изменения:

```bash
sudo netplan apply
```

### Шаг 10: Безопасность

⚠️ **Важные рекомендации по безопасности:**

1. **Используйте только необходимые порты:**
   - Откройте только порты 80 (HTTP) и 443 (HTTPS)
   - Не открывайте MySQL порт (3306) без необходимости

2. **Настройте fail2ban для защиты от атак:**
   ```bash
   sudo apt install fail2ban -y
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   ```

3. **Используйте сильные пароли** для всех сервисов

4. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

5. **Настройте брандмауэр на роутере** для ограничения доступа по IP (если возможно)

---

## 12. Настройка SSL с Let's Encrypt

> ⚠️ **Примечание**: SSL с Let's Encrypt работает только с реальными доменами, доступными из интернета. Для локальной разработки можно использовать самоподписанные сертификаты.

### Шаг 1: Установка Certbot и необходимых модулей Apache

```bash
# Установка Certbot
sudo apt install certbot python3-certbot-apache -y

# Включение необходимых модулей Apache
sudo a2enmod ssl
sudo a2enmod headers
sudo systemctl restart apache2
```

### Шаг 2: Настройка виртуального хоста для HTTPS

Убедитесь, что виртуальный хост настроен с реальным доменом:

```bash
sudo nano /etc/apache2/sites-available/dev.flowaxy.conf
```

Пример для реального домена `dev.flowaxy.com`:

```apache
<VirtualHost *:80>
    ServerName dev.flowaxy.com
    ServerAlias www.dev.flowaxy.com
    DocumentRoot /mnt/d/projects/domains/flowaxy.com/dev
    
    <Directory /mnt/d/projects/domains/flowaxy.com/dev>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
    
    ErrorLog ${APACHE_LOG_DIR}/dev.flowaxy_error.log
    CustomLog ${APACHE_LOG_DIR}/dev.flowaxy_access.log combined
    
    <FilesMatch \.php$>
        SetHandler application/x-httpd-php
    </FilesMatch>
</VirtualHost>
```

Активируйте и перезагрузите:

```bash
sudo a2ensite dev.flowaxy.conf
sudo apache2ctl configtest
sudo systemctl reload apache2
```

### Шаг 3: Получение SSL сертификата

#### Вариант A: Автоматическая настройка через Certbot (рекомендуется)

```bash
sudo certbot --apache -d dev.flowaxy.com -d www.dev.flowaxy.com
```

Certbot попросит:
- **Email**: введите email для уведомлений о истечении сертификата
- **Согласие с Terms of Service**: введите `Y` (Yes)
- **Share email с EFF**: введите `Y` или `N` (по желанию)
- **Редирект HTTP → HTTPS**: выберите `2` (Redirect) - **рекомендуется**

Certbot автоматически:
- Создаст SSL сертификат
- Настроит виртуальный хост для HTTPS (порт 443)
- Добавит редирект с HTTP на HTTPS
- Включит необходимые модули Apache

#### Вариант B: Получение сертификата без автоматической настройки Apache

Если хотите настроить Apache вручную:

```bash
# Получение сертификата без автоматической настройки
sudo certbot certonly --apache -d dev.flowaxy.com -d www.dev.flowaxy.com
```

### Шаг 4: Настройка HTTPS виртуального хоста (если использовали вариант B)

После получения сертификата добавьте HTTPS конфигурацию:

```bash
sudo nano /etc/apache2/sites-available/dev.flowaxy-ssl.conf
```

Вставьте следующую конфигурацию:

```apache
<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName dev.flowaxy.com
    ServerAlias www.dev.flowaxy.com
    DocumentRoot /mnt/d/OSPanel/home/flowaxy.com
    
    # SSL настройки
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/dev.flowaxy.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/dev.flowaxy.com/privkey.pem
    
    # Включаем HSTS (Strict Transport Security)
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    
    # Security Headers
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; img-src 'self' data: https:; font-src 'self' https://cdn.jsdelivr.net data:;"
    
    <Directory /mnt/d/OSPanel/home/flowaxy.com>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
    
    ErrorLog ${APACHE_LOG_DIR}/dev.flowaxy_ssl_error.log
    CustomLog ${APACHE_LOG_DIR}/dev.flowaxy_ssl_access.log combined
    
    <FilesMatch \.php$>
        SetHandler application/x-httpd-php
    </FilesMatch>
    
    # Кеширование статических файлов
    <IfModule mod_expires.c>
        ExpiresActive On
        ExpiresByType image/jpg "access plus 1 year"
        ExpiresByType image/jpeg "access plus 1 year"
        ExpiresByType image/gif "access plus 1 year"
        ExpiresByType image/png "access plus 1 year"
        ExpiresByType image/svg+xml "access plus 1 year"
        ExpiresByType text/css "access plus 1 month"
        ExpiresByType application/javascript "access plus 1 month"
        ExpiresByType font/woff "access plus 1 year"
        ExpiresByType font/woff2 "access plus 1 year"
    </IfModule>
</VirtualHost>
</IfModule>
```

Активируйте SSL конфигурацию:

```bash
sudo a2ensite dev.flowaxy-ssl.conf
sudo apache2ctl configtest
sudo systemctl reload apache2
```

### Шаг 5: Настройка автоматического обновления сертификатов

Let's Encrypt сертификаты действительны 90 дней. Certbot автоматически настраивает таймер для обновления.

#### Проверка статуса автоматического обновления

```bash
# Проверка статуса таймера
sudo systemctl status certbot.timer

# Проверка включен ли автозапуск
sudo systemctl is-enabled certbot.timer
```

Если таймер не активен, включите его:

```bash
# Включение автозапуска таймера
sudo systemctl enable certbot.timer

# Запуск таймера
sudo systemctl start certbot.timer

# Проверка статуса
sudo systemctl status certbot.timer
```

#### Тестирование обновления сертификата

Проверьте, что обновление работает (тестовый запуск):

```bash
# Тестовое обновление без применения изменений
sudo certbot renew --dry-run
```

#### Настройка автоматического обновления с перезагрузкой Apache

Создайте deploy hook для автоматической перезагрузки Apache после обновления:

```bash
sudo nano /etc/letsencrypt/renewal-hooks/deploy/reload-apache.sh
```

Вставьте:

```bash
#!/bin/bash
# Скрипт для автоматической перезагрузки Apache после обновления сертификата

systemctl reload apache2
```

Сделайте скрипт исполняемым:

```bash
sudo chmod +x /etc/letsencrypt/renewal-hooks/deploy/reload-apache.sh
```

#### Настройка cron для проверки обновлений (дополнительно)

```bash
sudo nano /etc/cron.d/certbot
```

Добавьте строку (если еще нет):

```
0 3 * * * certbot renew --quiet --deploy-hook "/bin/systemctl reload apache2"
```

### Шаг 6: Настройка уведомлений о скором истечении

Создайте скрипт для проверки срока действия сертификата:

```bash
sudo nano /usr/local/bin/certbot-notify.sh
```

Вставьте (замените email на свой):

```bash
#!/bin/bash
# Проверка истечения SSL сертификатов и отправка уведомлений

DOMAIN="dev.flowaxy.com"
CERT_PATH="/etc/letsencrypt/live/$DOMAIN/cert.pem"
EMAIL="your-email@example.com"

if [ -f "$CERT_PATH" ]; then
    EXPIRY_DATE=$(openssl x509 -enddate -noout -in "$CERT_PATH" | cut -d= -f2)
    EXPIRY_EPOCH=$(date -d "$EXPIRY_DATE" +%s)
    CURRENT_EPOCH=$(date +%s)
    DAYS_LEFT=$(( ($EXPIRY_EPOCH - $CURRENT_EPOCH) / 86400 ))
    
    if [ $DAYS_LEFT -lt 30 ]; then
        echo "SSL сертификат для $DOMAIN истекает через $DAYS_LEFT дней" | \
        mail -s "SSL Certificate Expiring Soon - $DOMAIN" "$EMAIL" 2>/dev/null || \
        echo "WARNING: SSL certificate for $DOMAIN expires in $DAYS_LEFT days"
    fi
fi
```

Сделайте скрипт исполняемым:

```bash
sudo chmod +x /usr/local/bin/certbot-notify.sh
```

Добавьте в cron (опционально):

```bash
sudo crontab -e
```

Добавьте строку:

```
0 9 * * * /usr/local/bin/certbot-notify.sh
```

### Шаг 7: Настройка Windows Firewall

Откройте PowerShell от имени администратора:

```powershell
# Разрешить HTTPS (порт 443)
netsh advfirewall firewall add rule name="Apache HTTPS" dir=in action=allow protocol=TCP localport=443

# Разрешить HTTP (порт 80)
netsh advfirewall firewall add rule name="Apache HTTP" dir=in action=allow protocol=TCP localport=80

# Проверка правил
netsh advfirewall firewall show rule name="Apache HTTPS"
netsh advfirewall firewall show rule name="Apache HTTP"
```

### Шаг 8: Проверка SSL сертификата

#### Проверка через браузер

Откройте: `https://dev.flowaxy.com`

В адресной строке должен отображаться значок замка (🔒).

#### Проверка через командную строку

```bash
# Проверка срока действия сертификата
sudo certbot certificates

# Детальная информация о сертификате
openssl x509 -in /etc/letsencrypt/live/dev.flowaxy.com/cert.pem -text -noout | grep -A 2 "Validity"

# Проверка через openssl
echo | openssl s_client -servername dev.flowaxy.com -connect dev.flowaxy.com:443 2>/dev/null | openssl x509 -noout -dates

# Проверка через curl
curl -I https://dev.flowaxy.com
```

### Шаг 9: Дополнительные настройки безопасности SSL

#### Отключение старых версий SSL/TLS

Отредактируйте конфигурацию SSL:

```bash
sudo nano /etc/apache2/mods-available/ssl.conf
```

Найдите секцию `<IfModule mod_ssl.c>` и добавьте/измените:

```apache
# Разрешить только современные протоколы (TLS 1.2 и выше)
SSLProtocol all -SSLv2 -SSLv3 -TLSv1 -TLSv1.1

# Современные шифры (приоритет безопасности)
SSLCipherSuite ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384
SSLHonorCipherOrder on

# Включить OCSP Stapling (улучшает производительность)
SSLUseStapling on
SSLStaplingCache "shmcb:logs/ssl_stapling(32768)"
```

Перезагрузите Apache:

```bash
sudo apache2ctl configtest
sudo systemctl reload apache2
```

### Шаг 10: Мониторинг и логирование

#### Просмотр логов SSL

```bash
# Логи Apache SSL
sudo tail -f /var/log/apache2/dev.flowaxy_ssl_error.log
sudo tail -f /var/log/apache2/dev.flowaxy_ssl_access.log

# Логи Certbot
sudo tail -f /var/log/letsencrypt/letsencrypt.log
```

#### Создание скрипта для мониторинга сертификатов

```bash
sudo nano /usr/local/bin/check-ssl-cert.sh
```

Вставьте:

```bash
#!/bin/bash
# Скрипт для проверки SSL сертификатов

DOMAIN="dev.flowaxy.com"
CERT_PATH="/etc/letsencrypt/live/$DOMAIN/cert.pem"

if [ -f "$CERT_PATH" ]; then
    EXPIRY_DATE=$(openssl x509 -enddate -noout -in "$CERT_PATH" | cut -d= -f2)
    EXPIRY_EPOCH=$(date -d "$EXPIRY_DATE" +%s)
    CURRENT_EPOCH=$(date +%s)
    DAYS_LEFT=$(( ($EXPIRY_EPOCH - $CURRENT_EPOCH) / 86400 ))
    
    echo "=== SSL Certificate Status for $DOMAIN ==="
    echo "Expiry Date: $EXPIRY_DATE"
    echo "Days Until Expiry: $DAYS_LEFT"
    
    if [ $DAYS_LEFT -lt 30 ]; then
        echo "⚠️  WARNING: Certificate expires in less than 30 days!"
        exit 1
    else
        echo "✅ Certificate is valid for $DAYS_LEFT more days"
        exit 0
    fi
else
    echo "❌ ERROR: Certificate file not found at $CERT_PATH"
    exit 1
fi
```

Сделайте исполняемым:

```bash
sudo chmod +x /usr/local/bin/check-ssl-cert.sh
```

Запустите для проверки:

```bash
sudo /usr/local/bin/check-ssl-cert.sh
```

### Шаг 11: Ручное обновление сертификата (при необходимости)

Если автоматическое обновление не сработало:

```bash
# Обновить все сертификаты
sudo certbot renew

# Обновить конкретный сертификат
sudo certbot renew --cert-name dev.flowaxy.com

# Обновить и перезагрузить Apache
sudo certbot renew --deploy-hook "systemctl reload apache2"
```

### Решение проблем с SSL

#### Проблема: Сертификат не обновляется автоматически

```bash
# Проверьте статус таймера
sudo systemctl status certbot.timer

# Включите и запустите таймер
sudo systemctl enable certbot.timer
sudo systemctl start certbot.timer

# Проверьте логи
sudo journalctl -u certbot.timer
```

#### Проблема: Ошибка при получении сертификата

```bash
# Проверьте, что порт 80 открыт и доступен из интернета
sudo netstat -tulpn | grep :80

# Проверьте DNS записи
nslookup dev.flowaxy.com

# Проверьте логи Certbot
sudo tail -f /var/log/letsencrypt/letsencrypt.log
```

### Дополнительные ресурсы

- [Let's Encrypt документация](https://letsencrypt.org/docs/)
- [Certbot документация](https://certbot.eff.org/docs/)
- [SSL Labs SSL Test](https://www.ssllabs.com/ssltest/) - проверить конфигурацию SSL

---

## 13. Установка Flowaxy CMS

### Шаг 1: Создание базы данных

База данных уже создана автоматически при настройке MySQL (см. раздел 4).

**Параметры базы данных:**
- **Хост:** `127.0.0.1` (локально) или IP Windows хоста для HeidiSQL
- **Порт:** `3306`
- **Имя базы:** `flowaxy_dev`
- **Пользователь:** `root`
- **Пароль:** `#Nt2GDaUm4r5rp8i`

### Шаг 2: Установка Flowaxy CMS через веб-интерфейс

1. **Откройте браузер** и перейдите: `http://dev.flowaxy.com/install`

2. **Следуйте инструкциям установщика:**
   - Шаг 1: Проверка требований
   - Шаг 2: Настройки базы данных
     - **Хост БД:** `127.0.0.1`
     - **Порт БД:** `3306`
     - **Имя БД:** `flowaxy_dev`
     - **Пользователь:** `root`
     - **Пароль:** `#Nt2GDaUm4r5rp8i`
     - **Префикс таблиц:** `flowaxy_` (по умолчанию)
   - Шаг 3: Создание таблиц
   - Шаг 4: Создание администратора
     - **Логин:** `admin` (или свой)
     - **Email:** `admin@dev.flowaxy.com` (или свой)
     - **Пароль:** (придумайте надежный пароль)
   - Шаг 5: Завершение установки

3. **После установки** система автоматически создаст файл `engine/data/database.ini` с настройками подключения.

### Шаг 3: Проверка установки

1. **Откройте админку:** `http://dev.flowaxy.com/admin`
2. **Войдите** с созданными учетными данными
3. **Проверьте работу** основных функций

### Шаг 4: Создание базы данных через SQL (альтернативный способ)

Если нужно создать базу данных вручную через HeidiSQL:

```sql
-- Создание базы данных
CREATE DATABASE IF NOT EXISTS `flowaxy_dev` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Проверка создания
SHOW DATABASES LIKE 'flowaxy_dev';
```

### Шаг 5: Импорт структуры базы данных (если есть готовый SQL)

Если у вас есть готовый SQL файл со структурой:

**Через HeidiSQL:**
1. Откройте подключение к базе `flowaxy_dev`
2. Выберите базу данных в списке слева
3. Файл → Загрузить SQL файл
4. Выберите файл SQL и выполните

**Через командную строку:**
```bash
# Импорт SQL файла
mysql -u root -p'#Nt2GDaUm4r5rp8i' flowaxy_dev < /path/to/dump.sql

# Или из WSL
mysql -u root -p'#Nt2GDaUm4r5rp8i' flowaxy_dev < /mnt/d/projects/domains/flowaxy.com/dev/.dev/data/core_tables.sql
```

---

## 14. Полезные команды

### WSL

```bash
# Перезапуск WSL (из PowerShell)
wsl --shutdown

# Получить IP адрес WSL
hostname -I

# Список установленных дистрибутивов
wsl --list --verbose

# Запуск конкретного дистрибутива
wsl -d Ubuntu
```

### Apache

```bash
# Статус Apache
sudo systemctl status apache2

# Запуск Apache
sudo systemctl start apache2

# Остановка Apache
sudo systemctl stop apache2

# Перезапуск Apache
sudo systemctl restart apache2

# Перезагрузка конфигурации (без перезапуска)
sudo systemctl reload apache2

# Проверка конфигурации Apache
sudo apache2ctl configtest

# Просмотр ошибок
sudo tail -f /var/log/apache2/error.log

# Просмотр логов конкретного сайта (dev.flowaxy.com)
sudo tail -f /var/log/apache2/dev.flowaxy_error.log
```

### MySQL/MariaDB

```bash
# Статус MySQL
sudo systemctl status mysql

# Запуск MySQL
sudo systemctl start mysql

# Остановка MySQL
sudo systemctl stop mysql

# Перезапуск MySQL
sudo systemctl restart mysql

# Подключение к MySQL
mysql -u root -p

# Создание базы данных
mysql -u root -p -e "CREATE DATABASE flowaxy CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# Импорт базы данных
mysql -u root -p flowaxy < /path/to/dump.sql

# Экспорт базы данных
mysqldump -u root -p flowaxy > /path/to/backup.sql
```

### PHP

```bash
# Версия PHP
php -v

# Конфигурация PHP
php -i

# Запуск встроенного сервера (для тестирования)
php -S localhost:8000

# Проверка установленных модулей PHP
php -m

# Путь к php.ini
php --ini
```

### Файлы и права доступа

```bash
# Изменить владельца
sudo chown -R $USER:$USER /path/to/directory

# Изменить права доступа
sudo chmod -R 755 /path/to/directory

# Для папок с записью
sudo chmod -R 775 /path/to/directory

# Рекурсивное изменение прав
sudo find /path/to/directory -type d -exec chmod 755 {} \;
sudo find /path/to/directory -type f -exec chmod 644 {} \;
```

---

## Решение проблем

### Проблема: Apache не запускается

```bash
# Проверьте конфигурацию
sudo apache2ctl configtest

# Проверьте логи
sudo tail -f /var/log/apache2/error.log

# Проверьте, не занят ли порт 80
sudo netstat -tulpn | grep :80
```

### Проблема: .htaccess не работает

1. Убедитесь, что `mod_rewrite` включен: `sudo a2enmod rewrite`
2. Проверьте `AllowOverride All` в конфигурации
3. Перезапустите Apache: `sudo systemctl restart apache2`

### Проблема: PHP файлы загружаются как текст

```bash
# 1. Проверьте, установлен ли модуль PHP для Apache
dpkg -l | grep libapache2-mod-php8.4

# 2. Если не установлен, установите его
sudo apt install libapache2-mod-php8.4 -y

# 3. Проверьте, что модуль PHP загружен в Apache
apache2ctl -M | grep php

# 4. Если модуль не найден, проверьте наличие конфигурации
ls -la /etc/apache2/mods-enabled/ | grep php

# 5. Перезапустите Apache
sudo systemctl restart apache2

# 6. Проверьте, что в конфигурации виртуального хоста есть:
<FilesMatch \.php$>
    SetHandler application/x-httpd-php
</FilesMatch>
```

**Примечание:** Модуль PHP активируется автоматически при установке `libapache2-mod-php8.4`. Команда `a2enmod php8.4` **не существует** и не нужна.

### Проблема: Медленная работа файлов из Windows

Рассмотрите возможность переноса проекта в файловую систему WSL (`~/projects`) для лучшей производительности.

### Проблема: Не могу подключиться к MySQL

```bash
# Проверьте статус MySQL
sudo systemctl status mysql

# Перезапустите MySQL
sudo systemctl restart mysql

# Попробуйте подключиться с sudo
sudo mysql -u root
```

---

## Дополнительные настройки

### Увеличение лимитов PHP для больших файлов

Отредактируйте `/etc/php/8.4/apache2/php.ini`:

```ini
upload_max_filesize = 256M
post_max_size = 256M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
```

### Настройка кэширования OPcache

В том же файле:

```ini
opcache.enable=1
opcache.memory_consumption=128
opcache.interned_strings_buffer=8
opcache.max_accelerated_files=10000
opcache.revalidate_freq=2
```

### Оптимизация Apache для производительности

Отредактируйте `/etc/apache2/apache2.conf`:

```apache
# Увеличьте лимиты
ServerLimit 16
MaxRequestWorkers 400
```

---

## 16. Полная очистка и переустановка WSL

> ⚠️ **Важно**: Если что-то пошло не так и нужно начать с чистого листа, следуйте этой инструкции для полной очистки и переустановки WSL.

---

### 🧹 Полная очистка WSL

#### Шаг 1: Удаление всех дистрибутивов WSL

Откройте **PowerShell от имени администратора** и выполните:

```powershell
# Просмотр всех установленных дистрибутивов
wsl --list --all
```

Пример вывода:
```
  NAME            STATE           VERSION
* Ubuntu          Running         2
  Debian          Stopped         2
  docker-desktop  Stopped         2
```

Удалите каждый дистрибутив:

```powershell
# Удаление конкретного дистрибутива
wsl --unregister Ubuntu
wsl --unregister Debian
wsl --unregister docker-desktop
wsl --unregister docker-desktop-data
```

⚠️ **Внимание**: Это удалит сам дистрибутив и его виртуальный диск (VHDX) со всеми данными!

#### Шаг 2: Остановка всех дистрибутивов WSL

Перед удалением убедитесь, что все дистрибутивы остановлены:

```powershell
# Принудительная остановка всех дистрибутивов
wsl --shutdown
```

Проверьте, что все остановлены:

```powershell
wsl --list --verbose
```

#### Шаг 3: Полное отключение компонентов WSL

Отключите компоненты Windows для WSL:

```powershell
# Отключение WSL
dism.exe /online /disable-feature /featurename:Microsoft-Windows-Subsystem-Linux /norestart

# Отключение платформы виртуальных машин
dism.exe /online /disable-feature /featurename:VirtualMachinePlatform /norestart
```

#### Шаг 4: Перезагрузка компьютера

После отключения компонентов **обязательно перезагрузите компьютер**:

```powershell
Restart-Computer
```

#### Шаг 5: Опционально: Удаление остатков вручную

После перезагрузки можно удалить оставшиеся каталоги (опционально):

**В Windows:**

```powershell
# Удаление каталогов дистрибутивов (замените <user> на ваше имя пользователя)
Remove-Item -Recurse -Force "C:\Users\<user>\AppData\Local\Packages\Canonical*" -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "C:\Users\<user>\AppData\Local\lxss" -ErrorAction SilentlyContinue
```

Или вручную через Проводник:
- `C:\Users\<ваше_имя>\AppData\Local\Packages\Canonical*` (для Ubuntu)
- `C:\Users\<ваше_имя>\AppData\Local\Packages\TheDebianProject*` (для Debian)
- `C:\Users\<ваше_имя>\AppData\Local\lxss` (если остался — редко)

#### Шаг 6: Очистка реестра (только для опытных пользователей)

⚠️ **Внимание**: Редактирование реестра может повредить систему! Делайте только если уверены.

```powershell
# Экспорт реестра перед редактированием (обязательно!)
reg export "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Lxss" "C:\wsl_registry_backup.reg"

# Удаление записей WSL из реестра (опционально)
reg delete "HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Lxss" /f
```

---

### 🔁 Чистая установка WSL

После полной очистки выполните установку заново:

#### Шаг 1: Включение компонентов WSL

Включите компоненты Windows для WSL:

```powershell
# Включение WSL
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart

# Включение платформы виртуальных машин
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
```

#### Шаг 2: Перезагрузка компьютера

**Обязательно перезагрузите компьютер** после включения компонентов:

```powershell
Restart-Computer
```

#### Шаг 3: Установка WSL2 как версии по умолчанию

После перезагрузки установите WSL2 как версию по умолчанию:

```powershell
# Установка WSL2 как версии по умолчанию
wsl --set-default-version 2
```

Если получите ошибку "WSL 2 requires an update to its kernel component", скачайте и установите обновление ядра WSL2:
- [Обновление ядра WSL2](https://aka.ms/wsl2kernel)

#### Шаг 4: Установка дистрибутива

**Самый простой способ — установка Ubuntu:**

```powershell
# Установка Ubuntu (последняя версия)
wsl --install -d Ubuntu
```

**Или выберите конкретную версию:**

```powershell
# Просмотр доступных дистрибутивов
wsl --install -l --online
```

Пример вывода:
```
NAME            FRIENDLY NAME
Ubuntu          Ubuntu
Debian          Debian GNU/Linux
kali-linux      Kali Linux Rolling
openSUSE-42     openSUSE Leap 42
SLES-12         SUSE Linux Enterprise Server v12
Ubuntu-16.04    Ubuntu 16.04 LTS
Ubuntu-18.04    Ubuntu 18.04 LTS
Ubuntu-20.04    Ubuntu 20.04 LTS
```

Установите выбранный дистрибутив:

```powershell
# Пример: установка Ubuntu 22.04
wsl --install -d Ubuntu-22.04
```

#### Шаг 5: Первый запуск после установки

После установки откройте установленный дистрибутив из меню Пуск или выполните:

```powershell
wsl -d Ubuntu
```

При первом запуске:
1. Введите имя пользователя (латиницей, без пробелов)
2. Введите пароль (символы не будут отображаться)
3. Подтвердите пароль

#### Шаг 6: Проверка установки

Проверьте, что все работает:

```powershell
# Проверка версии WSL
wsl --version

# Проверка установленных дистрибутивов
wsl --list --verbose

# Проверка версии WSL для конкретного дистрибутива
wsl -d Ubuntu --version
```

---

### ⚠️ Если ничего не помогает: Дополнительные способы очистки

#### Способ 1: Полная очистка через Windows Features

1. Откройте **Панель управления** → **Программы** → **Включение или отключение компонентов Windows**
2. Снимите галочки:
   - ☐ Подсистема Windows для Linux
   - ☐ Платформа виртуальных машин
3. Нажмите **ОК** и перезагрузите компьютер
4. После перезагрузки включите компоненты обратно

#### Способ 2: Очистка через PowerShell (более тщательная)

```powershell
# Остановка всех дистрибутивов
wsl --shutdown

# Удаление всех дистрибутивов
Get-ChildItem "HKCU:\Software\Microsoft\Windows\CurrentVersion\Lxss" | ForEach-Object {
    $guid = $_.PSChildName
    Write-Host "Removing $guid"
    Remove-Item "HKCU:\Software\Microsoft\Windows\CurrentVersion\Lxss\$guid" -Recurse -Force
}

# Отключение компонентов
dism.exe /online /disable-feature /featurename:Microsoft-Windows-Subsystem-Linux /norestart
dism.exe /online /disable-feature /featurename:VirtualMachinePlatform /norestart

# Перезагрузка
Restart-Computer
```

#### Способ 3: Очистка при использовании Docker Desktop

Если вы используете Docker Desktop, он также создает дистрибутивы WSL:

```powershell
# Удаление дистрибутивов Docker Desktop
wsl --unregister docker-desktop
wsl --unregister docker-desktop-data

# Остановка всех дистрибутивов
wsl --shutdown

# Проверка оставшихся дистрибутивов
wsl --list --all
```

---

### 🔧 После переустановки: Повторная настройка проекта

После чистой установки WSL выполните установку заново:

1. **Обновите систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **Выполните автоматический скрипт установки:**
   ```bash
   ~/install-flowaxy.sh
   ```
   
   Или следуйте инструкциям из разделов 3-14 данного руководства.

3. **Восстановите настройки:**
   - Настройте виртуальный хост Apache
   - Настройте hosts файл
   - Настройте подключение к MySQL
   - Импортируйте базу данных (если есть бэкап)

---

### 📋 Чек-лист полной очистки

- [ ] Остановлены все дистрибутивы WSL (`wsl --shutdown`)
- [ ] Удалены все дистрибутивы (`wsl --unregister`)
- [ ] Отключены компоненты WSL через DISM
- [ ] Перезагружен компьютер
- [ ] Удалены остатки в AppData (опционально)
- [ ] Включены компоненты WSL обратно
- [ ] Перезагружен компьютер
- [ ] Установлен WSL2 как версия по умолчанию
- [ ] Установлен дистрибутив Ubuntu
- [ ] Выполнена повторная настройка проекта

---

### 🆘 Решение проблем при переустановке

#### Проблема: "WSL 2 requires an update to its kernel component"

**Решение:**
1. Скачайте [обновление ядра WSL2](https://aka.ms/wsl2kernel)
2. Установите обновление
3. Перезагрузите компьютер
4. Попробуйте снова: `wsl --set-default-version 2`

#### Проблема: "The requested operation could not be completed due to a virtual disk system limitation"

**Решение:**
Это может быть связано с поврежденным VHDX файлом. Попробуйте:
```powershell
# Принудительное удаление дистрибутива
wsl --unregister <имя_дистрибутива> --force
```

#### Проблема: Дистрибутив не запускается после установки

**Решение:**
1. Проверьте, что WSL2 установлен правильно: `wsl --version`
2. Попробуйте запустить дистрибутив с версией WSL1:
   ```powershell
   wsl --set-version Ubuntu 1
   ```
3. Затем переключите обратно на WSL2:
   ```powershell
   wsl --set-version Ubuntu 2
   ```

#### Проблема: Компоненты WSL не отключаются/не включаются

**Решение:**
1. Проверьте, что вы запустили PowerShell от имени администратора
2. Попробуйте использовать альтернативный способ:
   ```powershell
   Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Windows-Subsystem-Linux
   Enable-WindowsOptionalFeature -Online -FeatureName VirtualMachinePlatform
   ```

---

## Полезные ссылки

- [Документация WSL](https://docs.microsoft.com/en-us/windows/wsl/)
- [Документация Apache](https://httpd.apache.org/docs/)
- [Документация PHP](https://www.php.net/docs.php)
- [Документация Certbot](https://certbot.eff.org/)

---

**Готово!** Теперь у вас настроена полноценная среда разработки на WSL2. 🚀
