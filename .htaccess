# Минимальная рабочая конфигурация Apache для CMS

# Включаем mod_rewrite
RewriteEngine On

# Обработка запросов к админке (должно быть первым)
# Перенаправление статических файлов админки (styles, scripts, images)
RewriteCond %{REQUEST_URI} ^/admin/(styles|scripts|images)/(.*)$
RewriteCond %{DOCUMENT_ROOT}/engine/skins/$1/$2 -f
RewriteRule ^admin/(styles|scripts|images)/(.*)$ /engine/skins/$1/$2 [L]

# Исключаем реальные файлы и директории
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Перенаправляем все запросы (включая админку) на index.php
RewriteRule ^(.*)$ index.php [QSA,L]

# Базовая безопасность
<Files ~ "^\.">
    Order allow,deny
    Deny from all
</Files>

<Files ~ "\.sql$">
    Order allow,deny
    Deny from all
</Files>

<Files ~ "\.log$">
    Order allow,deny
    Deny from all
</Files>

# Отключаем отображение директорий
Options -Indexes
